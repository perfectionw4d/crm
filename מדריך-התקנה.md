# מדריך התקנה — שקוף בחזית CRM

---

## שלב 1 — התקן Node.js

1. כנס לאתר: **https://nodejs.org**
2. הורד את הגרסה **LTS** (הצד השמאלי, המומלץ)
3. התקן — לחץ Next בכל המסכים
4. לאחר ההתקנה, פתח **שורת פקודה** (CMD) והקלד:
   ```
   node --version
   ```
   אמור לראות מספר גרסה כמו `v20.x.x` — זה אומר שהכל תקין.

---

## שלב 2 — חלץ את קובץ ה-ZIP

1. העתק את הקובץ `shakuf-crm-export.zip` למחשב החדש
2. לחץ עליו קליק ימני → **חלץ הכל** (Extract All)
3. בחר תיקייה נוחה, למשל: `C:\CRM\`
4. בתוך התיקייה תראה:
   ```
   crm-export/
   ├── server.js
   ├── package.json
   ├── package-lock.json
   ├── start.bat
   ├── public/
   │   ├── index.html
   │   └── login.html
   └── data/
       ├── contacts.json
       ├── orgs.json
       └── tasks.json
   ```

---

## שלב 3 — התקן את חבילות התוכנה

1. פתח **שורת פקודה** (CMD) בתיקיית `crm-export`
   - דרך קלה: בסייר הקבצים, פתח את תיקיית `crm-export`, לחץ על שורת הכתובת למעלה, הקלד `cmd` ולחץ Enter
2. הקלד:
   ```
   npm install
   ```
3. המתן כדקה — Node.js יוריד את כל הספריות הנדרשות
4. בסוף תראה משהו כמו: `added 350 packages`

---

## שלב 4 — הפעל את המערכת

**דרך קלה — לחץ פעמיים על:**
```
start.bat
```

**או מ-CMD:**
```
node server.js
```

תראה בשורת הפקודה:
```
✅ שרת פועל על פורט 3000
```

---

## שלב 5 — פתח את המערכת בדפדפן

פתח כל דפדפן (Chrome, Edge, Firefox) וכנס לכתובת:
```
http://localhost:3000
```

---

## ⚠️ חשוב לדעת

- **כל פעם** שרוצים להשתמש במערכת, צריך קודם להפעיל את `start.bat`
- הדפדפן חייב להיות פתוח **בזמן** שהשרת רץ
- הנתונים נשמרים בתיקיית `data/` — מומלץ לגבות אותה מדי פעם
- **WhatsApp**: אחרי התקנה חדשה צריך לסרוק QR Code מחדש (כניסה → WhatsApp)

---

## העברת נתונים מהמחשב הישן (אופציונלי)

אם רוצים להעביר את כל אנשי הקשר והנתונים מהמחשב הישן:

1. מהמחשב הישן — העתק את כל קבצי התיקייה `data/`
2. הדבק אותם בתיקיית `data/` על המחשב החדש (החלף את הקבצים הריקים)
3. הפעל מחדש את השרת

---

## עזרה בבעיות נפוצות

| בעיה | פתרון |
|------|-------|
| `node` לא מוכר | Node.js לא מותקן — חזור לשלב 1 |
| "Port already in use" | פתח Task Manager → מצא `node.exe` → סיים תהליך |
| הדף לא נטען | ודא ש-`start.bat` רץ ומוצג "שרת פועל" |
| שגיאה ב-npm install | ודא שאתה בתיקיית `crm-export` ונסה שוב |
